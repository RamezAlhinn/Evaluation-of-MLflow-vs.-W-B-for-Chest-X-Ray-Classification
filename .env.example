# Environment Configuration Template
# Copy this file to .env and fill in your values
# The .env file should be in .gitignore (don't commit secrets!)

# =============================================================================
# DATA CONFIGURATION
# =============================================================================

# Path to dataset
# Can be relative or absolute path
DATASET_PATH=Covid19-dataset

# =============================================================================
# MLFLOW CONFIGURATION
# =============================================================================

# MLflow tracking server URI
# Leave empty to use local ./mlruns directory
# Examples:
#   - Local: (leave empty)
#   - Remote: http://mlflow-server:5000
#   - Databricks: databricks://profile-name
MLFLOW_TRACKING_URI=

# MLflow experiment name
# Override default experiment name
MLFLOW_EXPERIMENT_NAME=chest-xray-classification

# =============================================================================
# WEIGHTS & BIASES CONFIGURATION
# =============================================================================

# W&B API key for authentication
# Get from: https://wandb.ai/authorize
WANDB_API_KEY=

# W&B project name
WANDB_PROJECT=chest-xray-classification

# W&B entity (team or username)
# Leave empty to use your personal account
WANDB_ENTITY=

# W&B mode: online, offline, or disabled
# - online: Sync to cloud in real-time
# - offline: Save locally, sync later with `wandb sync`
# - disabled: Don't track at all
WANDB_MODE=online

# =============================================================================
# TRAINING CONFIGURATION
# =============================================================================

# Device to use for training
# Options: cpu, cuda, cuda:0, cuda:1, mps (Apple Silicon)
# Leave empty to auto-detect (CUDA if available, else CPU)
DEVICE=

# Random seed for reproducibility
# Set to same value across runs for reproducible results
RANDOM_SEED=42

# =============================================================================
# KAGGLE CONFIGURATION (for dataset download)
# =============================================================================

# Kaggle username and API key
# Get from: https://www.kaggle.com/settings -> API -> Create New API Token
# This downloads a kaggle.json file with these values
KAGGLE_USERNAME=
KAGGLE_KEY=

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log file path (optional)
# Leave empty to only log to console
LOG_FILE=

# =============================================================================
# NOTES
# =============================================================================

# Why use environment variables?
# 1. Security: Keep secrets out of code and version control
# 2. Flexibility: Different configs for dev/staging/production
# 3. Portability: Works across different machines/environments
# 4. Convenience: Change configuration without modifying code
#
# How to use:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your values in .env
# 3. The application will automatically load from .env
# 4. Never commit .env to git (it's in .gitignore)
#
# Loading in Python:
#   from dotenv import load_dotenv
#   load_dotenv()  # Load .env file
#   value = os.getenv("VARIABLE_NAME", "default_value")
